VAL = {
    '+': lambda x, y: x + y,
    '-': lambda x, y: x - y,
    '*': lambda x, y: x * y,
    '/': lambda x, y: x / y,
    '**': lambda x, y: x ** y
}

ORDER = {'+': 1, '-': 1, '*': 2, '/': 2, '**': 3}
# EXAMPLE = "ПРИМЕР" 